pipeline {
    agent any
	
	options { skipDefaultCheckout(true) }
	
    stages {
        stage('Build') {
			agent any
			
			options { skipDefaultCheckout(true) }
			
            steps {                
                echo 'Hello World.......'
            }
        }
    }
}
